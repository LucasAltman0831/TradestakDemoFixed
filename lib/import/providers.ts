import {requireEnv} from '@/lib/env';
export async function discoverSuppliers(query:string,location:string){const key=requireEnv('GOOGLE_PLACES_API_KEY');const r=await fetch('https://places.googleapis.com/v1/places:searchText',{method:'POST',headers:{'content-type':'application/json','X-Goog-Api-Key':key,'X-Goog-FieldMask':'places.id,places.displayName,places.formattedAddress,places.websiteUri,places.nationalPhoneNumber,places.primaryType'},body:JSON.stringify({textQuery:`${query} in ${location}`,pageSize:20})});if(!r.ok)throw new Error(`Provider returned ${r.status}`);const j=await r.json();return (j.places||[]).map((p:any)=>({external_id:p.id,name:p.displayName?.text||'Unnamed supplier',trade_category:p.primaryType||query,city:location,state:null,website:p.websiteUri||null,phone:p.nationalPhoneNumber||null,service_area:location,description:null,source_name:'Google Places',source_url:p.id?`https://www.google.com/maps/search/?api=1&query_place_id=${encodeURIComponent(p.id)}`:null}));}
