import {PolicyPage} from '@/components/PolicyPage';
export default function Page(){return <PolicyPage eyebrow="TRANSPARENT SCORING" title="SourceMetric Score Methodology" intro="A consistent 0–100 summary based only on eligible verified evaluations." sections={[['Eligibility','An evaluation is included only when the business is verified, the relationship is verified, and the associated review is approved and eligible. Payments, supplier edits, and anonymous activity never affect a score.'],['Calculation','Quality, reliability, delivery or timeliness, communication or service, and value each receive equal 20% weight. Category values are arithmetic means of eligible evaluations, rounded to whole numbers. The overall score is the mean of those five category values, rounded to a whole number.'],['Minimum sample','A numeric score and would-work-again percentage appear only after at least three eligible verified evaluations. Before then, the profile says “Building performance history” and shows the eligible evaluation count.'],['Sample and context','Every score display includes its eligible evaluation count. The would-work-again percentage is the share of eligible reviewers answering yes. Scores are decision-support signals and do not guarantee future performance.'],['Recency','V1 uses consistent equal weighting and displays review dates for context. SourceMetric may add a documented recency model later, but will not silently change the calculation.']]}/>}
