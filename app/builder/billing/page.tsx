import {requireRole} from '@/lib/auth';import {Sidebar} from '@/components/Sidebar';import {PortalButton} from '@/components/PricingButtons';import Link from 'next/link';export default async function Page(){const {user,supabase}=await requireRole(['builder']);const {data}=await supabase.from('subscriptions').select('*').eq('user_id',user.id).maybeSingle();return <main className="shell"><Sidebar role="builder"/><section className="workspace"><h1>Billing</h1><div className="panel"><p><b>Plan:</b> {data?.plan||'builder_free'}</p><p><b>Status:</b> {data?.status||'free'}</p>{data?.stripe_customer_id?<PortalButton/>:<Link className="button" href="/pricing">Upgrade to Pro</Link>}</div></section></main>}
