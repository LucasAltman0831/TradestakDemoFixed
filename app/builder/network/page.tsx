import Link from 'next/link';import {requireRole} from '@/lib/auth';import {Sidebar} from '@/components/Sidebar';import {SupplierCard} from '@/components/SupplierCard';import {WorkspaceHeading} from '@/components/WorkspaceHeading';import type {Supplier} from '@/lib/types';export default async function Page(){const {user,supabase}=await requireRole(['business']);const {data}=await supabase.from('saved_suppliers').select('supplier_profiles(*)').eq('business_user_id',user.id);const rows=(data||[]).map((r:any)=>r.supplier_profiles).filter(Boolean) as Supplier[];return <main className="shell"><Sidebar role="business"/><section className="workspace"><WorkspaceHeading eyebrow="SUPPLIER NETWORK" title="Saved suppliers" description="Keep trusted suppliers organized and ready for future sourcing needs." action={<Link className="button" href="/marketplace">Discover suppliers</Link>}/><div className="marketGrid">{rows.map(s=><SupplierCard key={s.id} supplier={s}/>)}</div>{!rows.length&&<div className="empty">Your saved supplier network is empty. Explore the marketplace to add trusted partners.</div>}</section></main>}
