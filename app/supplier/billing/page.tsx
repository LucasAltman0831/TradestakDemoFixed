import {requireRole} from '@/lib/auth';import {BillingExperience} from '@/components/BillingExperience';export default async function Page({searchParams}:{searchParams:Promise<{checkout?:string}>}){const [{user,supabase},params]=await Promise.all([requireRole(['supplier']),searchParams]);const {data}=await supabase.from('subscriptions').select('plan,status,current_period_end,stripe_customer_id').eq('user_id',user.id).maybeSingle();return <BillingExperience role="supplier" subscription={data} checkout={params.checkout}/>}
