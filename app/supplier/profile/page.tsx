import Link from 'next/link';import {requireRole} from '@/lib/auth';import {Sidebar} from '@/components/Sidebar';import {SupplierProfileForm} from '@/components/SupplierProfileForm';import {WorkspaceHeading} from '@/components/WorkspaceHeading';export default async function Page(){const {user,supabase}=await requireRole(['supplier']);const {data}=await supabase.from('supplier_profiles').select('*').eq('owner_id',user.id).maybeSingle();return <main className="shell"><Sidebar role="supplier"/><section className="workspace"><WorkspaceHeading eyebrow="COMPANY IDENTITY" title="Company profile" description="Keep your capabilities, service area, and company information accurate for builders."/>{data?<SupplierProfileForm profile={data}/>:<div className="panel emptyState"><h2>Claim a company to begin</h2><p>An approved company claim unlocks profile editing and reputation management.</p><Link className="button" href="/supplier/claim">Find your company</Link></div>}</section></main>}
