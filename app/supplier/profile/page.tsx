import {requireRole} from '@/lib/auth';import {Sidebar} from '@/components/Sidebar';import {SupplierProfileForm} from '@/components/SupplierProfileForm';export default async function Page(){const {user,supabase}=await requireRole(['supplier']);const {data}=await supabase.from('supplier_profiles').select('*').eq('owner_id',user.id).maybeSingle();return <main className="shell"><Sidebar role="supplier"/><section className="workspace"><h1>Company profile</h1>{data?<SupplierProfileForm profile={data}/>:<div className="panel"><p>You must have an approved claim before editing a profile.</p><a className="button" href="/supplier/claim">Claim a profile</a></div>}</section></main>}
