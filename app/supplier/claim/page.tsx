import {requireRole} from '@/lib/auth';import {Sidebar} from '@/components/Sidebar';import {ClaimForm} from '@/components/SupplierProfileForm';export default async function Page(){const {supabase}=await requireRole(['supplier']);const {data}=await supabase.from('supplier_profiles').select('id,name,trade_category,city,state').eq('claimed',false).eq('is_public',true).order('name').limit(300);return <main className="shell"><Sidebar role="supplier"/><section className="workspace"><h1>Claim your supplier profile</h1><p>Search TradeStak's existing company records and submit an ownership request.</p><ClaimForm suppliers={data||[]}/></section></main>}
