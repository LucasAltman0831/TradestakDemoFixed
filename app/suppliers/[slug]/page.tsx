import Link from 'next/link';import {notFound} from 'next/navigation';import {BadgeCheck,Building2,Globe2,MapPin,ShieldCheck,Sparkles} from 'lucide-react';import {Nav} from '@/components/Nav';import {createClient} from '@/lib/supabase/server';import {findDemoSupplier} from '@/lib/demo-suppliers';import styles from './supplier.module.css';
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const supabase=await createClient();const {data}=await supabase.from('supplier_profiles').select('*').eq('slug',slug).eq('is_public',true).maybeSingle();const supplier=data??findDemoSupplier(slug);if(!supplier)notFound();const score=supplier.score??0;const unclaimed=!supplier.claimed;return <><Nav/><main className={styles.page}><section className={styles.hero}><div className={styles.identity}><span>{supplier.name.split(' ').map((part:string)=>part[0]).slice(0,2).join('')}</span><div><p>{supplier.trade_category} · {supplier.city}, {supplier.state}</p><h1>{supplier.name}</h1><div>{supplier.verified?<b className={styles.verified}><BadgeCheck/>Verified company</b>:<b className={styles.unclaimed}><Building2/>Unclaimed Profile</b>}</div></div></div><div className={styles.score}><small>TRADESTAK SCORE</small><strong>{score}</strong><span>/100</span><p>Estimated reputation</p></div></section><section className={styles.layout}><div><article className={styles.about}><span>COMPANY OVERVIEW</span><h2>Known in the network.<br/>Ready to be claimed.</h2><p>{supplier.description||'This supplier profile contains foundational company and reputation information.'}</p><div className={styles.facts}><div><MapPin/><span>Location<b>{supplier.city}, {supplier.state}</b></span></div><div><Globe2/><span>Service area<b>{supplier.service_area||'Not provided'}</b></span></div><div><ShieldCheck/><span>Evaluations<b>{supplier.review_count??0} verified</b></span></div></div></article><article className={styles.performance}><header><div><span>REPUTATION SIGNALS</span><h2>Performance intelligence</h2></div><Sparkles/></header>{[['Quality',supplier.quality_score],['Delivery',supplier.delivery_score],['Communication',supplier.communication_score]].map(([label,value])=><div key={String(label)}><span>{label}</span><i><em style={{width:`${value??0}%`}}/></i><b>{value??'—'}</b></div>)}</article></div><aside className={styles.claimCard}>{unclaimed?<><div className={styles.claimIcon}><Building2/></div><span>THIS COMPANY IS ALREADY ON TRADESTAK</span><h2>Your company is already here.<br/>Claim your reputation.</h2><p>Take ownership of company details, respond to verified feedback, and show builders what your team has earned.</p><Link href={`/claim/${supplier.slug}`}>Claim This Profile</Link><small><ShieldCheck/>Authorization is reviewed before ownership is granted.</small></>:<><div className={styles.claimIcon}><BadgeCheck/></div><span>OWNERSHIP CONFIRMED</span><h2>This company profile is claimed.</h2><p>Company information is managed by an authorized representative.</p></>}<div className={styles.source}><b>Public information notice</b><p>This profile was created from public business information and TradeStak network signals.</p></div></aside></section></main></>}
