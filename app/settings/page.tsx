import {requireRole} from '@/lib/auth';import {Sidebar} from '@/components/Sidebar';export default async function Page(){const {profile}=await requireRole(['builder','supplier','admin']);return <main className="shell"><Sidebar role={profile.role}/><section className="workspace"><h1>Account settings</h1><div className="panel"><p><b>Email:</b> {profile.email}</p><p><b>Role:</b> {profile.role}</p><p>Password changes are available through the forgot-password flow.</p></div></section></main>}
