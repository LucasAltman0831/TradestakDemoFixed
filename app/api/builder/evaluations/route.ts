import {NextResponse} from 'next/server';import {getViewer} from '@/lib/auth';import {evaluationSchema} from '@/lib/validation';
export async function POST(req:Request){const {user,profile,supabase}=await getViewer();if(!user||profile?.role!=='builder')return NextResponse.json({error:'Builder account required.'},{status:403});const p=evaluationSchema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:p.error.issues[0]?.message||'Invalid evaluation.'},{status:400});const {error}=await supabase.from('evaluations').upsert({builder_user_id:user.id,...p.data},{onConflict:'builder_user_id,supplier_profile_id'});return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true});}
