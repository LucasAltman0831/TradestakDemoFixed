import Link from 'next/link';import {BadgeCheck,MapPin} from 'lucide-react';import type {Supplier} from '@/lib/types';
export function SupplierCard({supplier}:{supplier:Supplier}){return <article className="supplierCard"><div className="supplierTop"><div><div className="eyebrow">{supplier.trade_category||'Construction supplier'}</div><h3><Link href={`/suppliers/${supplier.slug}`}>{supplier.name}</Link> {supplier.verified&&<BadgeCheck size={16}/>}</h3><p><MapPin size={13}/>{[supplier.city,supplier.state].filter(Boolean).join(', ')||'Service area available'}</p></div><div className="score"><div><strong>{supplier.score??'—'}</strong><small>TRADESTAK</small></div></div></div><div className="supplierBottom"><span>{supplier.review_count||0} verified evaluations</span><span>{supplier.claimed?'Claimed':'Unclaimed'}</span></div><div className="intel"><div><small>Quality</small><b>{supplier.quality_score??'—'}</b></div><div><small>Delivery</small><b>{supplier.delivery_score??'—'}</b></div><div><small>Communication</small><b>{supplier.communication_score??'—'}</b></div></div></article>}
