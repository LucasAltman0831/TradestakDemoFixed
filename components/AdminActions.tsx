'use client';import {useState} from 'react';import {StatusMessage} from './StatusMessage';
export function ClaimReview({id}:{id:string}){const [m,setM]=useState('');async function act(action:'approve'|'reject'){const r=await fetch(`/api/admin/claims/${id}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action})});const j=await r.json();setM(r.ok?`Claim ${action}d.`:j.error||'Action failed.');if(r.ok)location.reload();}return <div><button className="button small" onClick={()=>act('approve')}>Approve</button> <button className="ghost small" onClick={()=>act('reject')}>Reject</button><StatusMessage message={m}/></div>}
export function ImportForm(){const [m,setM]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/admin/import',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});const j=await r.json();setM(r.ok?`Imported ${j.imported} suppliers.`:j.error||'Import failed.');}return <form className="panel" onSubmit={submit}><label>Trade or supplier keyword</label><input name="query" placeholder="roofing supplier" required/><label>Market</label><input name="location" placeholder="Huntsville, Alabama" required/><button className="button">Discover suppliers</button><StatusMessage message={m}/><p><small>This optional importer requires a Google Places API key and must be used according to Google's terms. Imported records retain source attribution.</small></p></form>}
