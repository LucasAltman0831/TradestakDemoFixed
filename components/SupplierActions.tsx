'use client';import {useState} from 'react';import {StatusMessage} from './StatusMessage';
export function SaveSupplierButton({id}:{id:string}){const [m,setM]=useState('');async function save(){const r=await fetch('/api/builder/saved',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({supplier_id:id})});const j=await r.json();setM(r.ok?'Supplier saved.':j.error||'Unable to save.');}return <><button className="button" onClick={save}>Save supplier</button><StatusMessage message={m} error={m.includes('Unable')}/></>}
export function EvaluationForm({id}:{id:string}){const [m,setM]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const body={supplier_profile_id:id,quality:Number(f.get('quality')),delivery:Number(f.get('delivery')),communication:Number(f.get('communication')),notes:String(f.get('notes'))};const r=await fetch('/api/builder/evaluations',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const j=await r.json();setM(r.ok?'Evaluation saved.':j.error||'Unable to save evaluation.');if(r.ok)e.currentTarget.reset();}return <form onSubmit={submit}><h3>Evaluate this supplier</h3><label>Quality (1–100)</label><input name="quality" type="number" min="1" max="100" required/><label>Delivery (1–100)</label><input name="delivery" type="number" min="1" max="100" required/><label>Communication (1–100)</label><input name="communication" type="number" min="1" max="100" required/><label>Notes</label><textarea name="notes" rows={4}/><button className="button full">Submit verified evaluation</button><StatusMessage message={m}/></form>}
