'use client';
import {useState} from 'react';
import {StatusMessage} from './StatusMessage';

export function SaveSupplierButton({id}:{id:string}){const [message,setMessage]=useState('');async function save(){const response=await fetch('/api/builder/saved',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({supplier_id:id})});const result=await response.json();setMessage(response.ok?'Supplier saved.':result.error||'Unable to save supplier.');}return <><button className="button" onClick={save}>Save supplier</button><StatusMessage message={message} error={message.startsWith('Unable')}/></>}

export function EvaluationForm({id}:{id:string}){const [message,setMessage]=useState('');const [publish,setPublish]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const body={supplier_profile_id:id,quality:Number(form.get('quality')),delivery:Number(form.get('delivery')),communication:Number(form.get('communication')),notes:String(form.get('notes')),publish_review:publish,project_name:String(form.get('project_name')),project_type:String(form.get('project_type')),review_body:String(form.get('review_body'))};const response=await fetch('/api/builder/evaluations',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const result=await response.json();setMessage(response.ok?'Evaluation submitted.':result.error||'Unable to save evaluation.');if(response.ok)event.currentTarget.reset();}return <form onSubmit={submit}><h3>Evaluate this supplier</h3><label>Quality (1–100)</label><input name="quality" type="number" min="1" max="100" required/><label>Delivery (1–100)</label><input name="delivery" type="number" min="1" max="100" required/><label>Service (1–100)</label><input name="communication" type="number" min="1" max="100" required/><label>Private notes</label><textarea name="notes" rows={3}/><label style={{display:'flex',gap:8,alignItems:'center'}}><input type="checkbox" style={{width:'auto'}} checked={publish} onChange={event=>setPublish(event.target.checked)}/>Publish a written buyer review</label>{publish?<><label>Engagement name<input name="project_name" maxLength={160}/></label><label>Engagement type<input name="project_type" maxLength={100}/></label><label>Public review<textarea name="review_body" rows={4} required maxLength={2000}/></label></>:null}<button className="button full">Submit supplier evaluation</button><StatusMessage message={message} error={message.startsWith('Unable')}/><small>Published reviews are labeled “Buyer submitted” until SourceMetric verifies the business relationship.</small></form>}
