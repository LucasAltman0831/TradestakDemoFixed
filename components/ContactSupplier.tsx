'use client';
import {useState} from 'react';import {StatusMessage} from './StatusMessage';
export function ContactSupplier({supplierId}:{supplierId:string}){const [message,setMessage]=useState('');async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const payload=Object.fromEntries(form);const response=await fetch('/api/inquiries',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({supplier_profile_id:supplierId,...payload})});const result=await response.json();setMessage(response.ok?'Inquiry sent privately. You can track replies in your workspace.':result.error||'Unable to send inquiry.');if(response.ok)event.currentTarget.reset()}return <form onSubmit={submit}><h3>Contact this supplier</h3><label>Subject<input name="subject" required minLength={3} maxLength={160}/></label><label>Message<textarea name="message" required minLength={10} maxLength={5000} rows={5}/></label><label>Category or capability<input name="category" maxLength={120}/></label><label>Approximate need<input name="approximate_need" maxLength={240}/></label><label>Timeline<input name="timeline" maxLength={120}/></label><label>Preferred contact method<select name="preferred_contact_method"><option>Email</option><option>Phone</option><option>SourceMetric message</option></select></label><button className="button full">Send inquiry</button><StatusMessage message={message} error={message.startsWith('Unable')}/></form>}
