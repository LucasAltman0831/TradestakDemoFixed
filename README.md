# TradeStak Demo V1

This repository is for design experimentation only. It uses seeded local data, does not require Supabase or Stripe, and must not be used to accept real accounts, claims, or payments.

Run `npm install && npm run dev`.
